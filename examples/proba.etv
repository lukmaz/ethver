user "U";
user "M";

CONTRACT_BALANCE_MAX = 4;
CONTRACT_BALANCE_INIT = 2;
USER_BALANCE_MAX = 4;
USER0_BALANCE_INIT = 0;
USER1_BALANCE_INIT = 0;
MAX_VALUE = 1;


contract RPS {
}

communication {
  uint(3) a;
  uint(3) x;
  uint(3) tab[3];
  uint(3) pom;

  function set(uint(2) y) {
    x = random(3);
    a = x;
    tab[x] = 1;
  }
}

scenario U {
}

scenario M {
}
