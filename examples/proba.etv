user "U";
user "M";

CONTRACT_BALANCE_MAX = 4;
CONTRACT_BALANCE_INIT = 2;
USER_BALANCE_MAX = 4;
USER0_BALANCE_INIT = 0;
USER1_BALANCE_INIT = 0;
MAX_VALUE = 1;


contract RPS {
}

communication {
  uint(3) a;
  uint(3) x;

  function set(uint(3) y) {
    if (a == 1)
    {
      x = 1;
    }
    if (y == 1)
    {
      x = 2;
    }
  }
}

scenario U {
}

scenario M {
}

