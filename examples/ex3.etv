contract Bank {
    uint(2000) pot;
    
    function deposit() {
      pot = msg.value;
    }
    
    function withdraw() {
      msg.sender.send(pot);
    }
}

deposit.sendTransaction({from: "0xabcde", value: 1000});

withdraw.sendTransaction({from: "0xabcde", value: 0});